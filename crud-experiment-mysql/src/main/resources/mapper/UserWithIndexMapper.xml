<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tomatos.lab.crud.mysql.mapper.UserWithIndexMapper">

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO t_user_with_index (user_id, username, email, phone, address, status, create_time, update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.userId}, #{item.username}, #{item.email}, #{item.phone}, #{item.address}, #{item.status}, #{item.createTime}, #{item.updateTime})
        </foreach>
    </insert>

</mapper>
